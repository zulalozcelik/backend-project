# 🔐 ENVIRONMENT VARIABLES - ÇEVRESEL DEĞİŞKENLER
# Bu dosya .env dosyanız için bir şablon/örnektir
# .env dosyanızı oluşturmak için bu dosyayı kopyalayın ve değerleri doldurun

# 📋 KULLANIM:
# 1. Bu dosyayı .env olarak kopyalayın
# 2. Aşağıdaki değerleri gerçek değerlerle doldurun
# 3. .env dosyasını asla Git'e commit etmeyin!

# =============================================================================
# 🗄️ DATABASE CONFIGURATION - Veritabanı Ayarları
# =============================================================================

# PostgreSQL bağlantı string'i
# Format: postgresql://[kullanıcı]:[şifre]@[host]:[port]/[veritabanı_adı]
DATABASE_URL=postgresql://postgres:password@localhost:5432/mydatabase

# =============================================================================
# 🔐 SECURITY - Güvenlik Ayarları
# =============================================================================

# JWT token şifreleme için secret key
# ⚠️ ÖNEMLİ: Production'da uzun, rastgele ve güvenli bir değer kullanın!
# Örnek üretmek için: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# 📦 CACHE - Önbellek Ayarları
# =============================================================================

# Redis bağlantı string'i
# Format: redis://[host]:[port]
REDIS_URL=redis://localhost:6379

# Redis şifre gerekiyorsa:
# REDIS_URL=redis://:password@localhost:6379

# =============================================================================
# 📚 API DOCUMENTATION - API Dokümantasyonu
# =============================================================================

# Swagger UI için kullanıcı adı
# Bu sayede API dokümantasyonunuza sadece yetkili kişiler erişebilir
SWAGGER_USER=admin

# Swagger UI için şifre
SWAGGER_PASSWORD=admin123

# =============================================================================
# 🌍 APPLICATION - Uygulama Ayarları
# =============================================================================

# Uygulamanın çalışacağı ortam
# İzin verilen değerler: development, staging, production
NODE_ENV=development

# Uygulamanın çalışacağı port numarası (opsiyonel, varsayılan: 3000)
PORT=3000

# =============================================================================
# 💡 NOTLAR:
# =============================================================================
#
# 1. .env dosyası GIT'e eklenmemelidir (.gitignore'da olmalı)
# 2. Hassas bilgiler (şifreler, secret key'ler) asla paylaşılmamalı
# 3. Her ortam için (dev, staging, prod) farklı değerler kullanın
# 4. Production'da tüm secret'lar güçlü ve benzersiz olmalıdır
